# API Configuration
VITE_API_BASE_URL=https://your-railway-backend.railway.app/api

# NOTE: the Slack bot token is a BACKEND secret and must never be exposed to the
# browser — do not add it here with a VITE_ prefix. See backend/config/.env.example.
